<h1>Today's View</h1>

<% @reminders.each do |r| %>
  <% if r.start_date.strftime("%m/%d/%Y") == Date.today.strftime("%m/%d/%Y") %>
    <div>
      <%= render 'reminder' %>
    </div>
  <% end %>
<% end %>

<%= render 'form' %>
